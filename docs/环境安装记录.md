# 云味餐厅环境安装记录

更新时间：2026-08-19

这份记录用于后续清理环境时判断哪些路径属于本项目。删除前先确认项目不再需要对应运行时或依赖。

## D 盘项目环境

| 路径 | 内容 | 当前用途 | 是否可直接删除 |
| --- | --- | --- | --- |
| `D:\workspace\cloud-taste\.tools\node-v12.22.0\nodejs` | Node.js `12.22.0`、npm `6.14.11`、npx | 课程 Vue 2/Vue CLI 3 前端运行 | 可以，但删除后源码前端不能直接启动 |
| `D:\workspace\cloud-taste\.tools\npm-7.24.2` | 项目专用 npm `7.24.2` | 按课程 `package-lock.json` 安装旧版依赖 | 可以，但重新安装依赖前需要恢复或重新安装 |
| `D:\workspace\cloud-taste\admin-web\project-sky-admin-vue-ts\node_modules` | 前端 npm 依赖 | Vue 源码开发、构建 | 可以重建，删除后需运行依赖安装脚本 |
| `D:\workspace\cloud-taste\admin-web\project-sky-admin-vue-ts\dist` | 前端生产构建产物 | 前端源码构建结果，可重新生成 | 可以重建，删除不影响源码 |
| `D:\workspace\cloud-taste\.maven-repository` | Maven 项目依赖缓存 | 后端构建加速 | 可以，但会重新下载依赖 |
| `D:\workspace\cloud-taste\admin-dist\nginx-1.20.2` | Nginx 和打包管理端 | 运行课程管理端成品 | 不建议删除，除非不再使用 Nginx 成品 |
| `D:\workspace\cloud-taste\backend\sky-take-out\sky-server\src\main\resources\application-local.yml` | 本机数据库密码、JWT 等配置 | 后端自动连接本机服务 | 可以删除，删除后需重新填写或使用环境变量 |

## D 盘项目文件

| 路径 | 内容 |
| --- | --- |
| `D:\workspace\cloud-taste\backend\sky-take-out` | Java/Spring Boot 后端 |
| `D:\workspace\cloud-taste\admin-web\project-sky-admin-vue-ts` | Vue 管理后台源码 |
| `D:\workspace\cloud-taste\database\cloud_taste.sql` | 数据库初始化脚本 |
| `D:\workspace\cloud-taste\scripts` | 构建、启动、停止和前端依赖安装脚本 |
| `D:\workspace\cloud-taste\docs\环境运行指南.md` | 日常打开和运行方式 |

## 本次 C 盘处理

已删除：

```text
C:\Users\LAN\AppData\Local\Cypress
```

它是课程 Cypress 测试浏览器缓存，约 414.7 MB；本项目的前端开发、构建和普通运行不依赖它。

未修改：

```text
C:\Users\LAN\AppData\Roaming\npm
```

该目录最后修改时间早于本次任务，无法证明由本项目产生，因此保留。

本次没有修改全局 Node 22，也没有修改全局 PATH。

## 本次 D 盘清理

已删除：

```text
D:\Caches\npm\cloud-taste-node_modules-failed-node22-20260819
```

这是 Node 22 安装失败后保留的前端依赖现场，约 589.2 MB，已确认不再使用。

已删除：

此前安装过程留下的 `D:\Caches\npm\cloud-taste-package-lock-original.tmp` 已确认不参与运行并删除。

## 重新安装前端依赖

从项目根目录执行：

```powershell
cd D:\workspace\cloud-taste
.\scripts\install-admin-deps.ps1
```

该脚本使用 D 盘 Node 12/npm 7，临时修复课程 lockfile 的旧镜像地址，安装完成后恢复原始 lockfile，并跳过 Cypress 浏览器下载。

2026-08-19 已实测脚本成功安装 2150 个锁定依赖；脚本会在恢复 lockfile 后自动删除 C 盘临时备份。

## 当前全局版本参考

本次任务开始时检测到：

```text
全局 Node：D:\nodeJS\node.exe，v22.16.0
全局 npm：10.9.2
```

它们没有被本项目替换。项目启动脚本会在当前进程中临时优先使用 D 盘 Node 12。
